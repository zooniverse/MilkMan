<ul class="status-list">
  <% @counts.each do |key, value| %>
  <li><b><%= key %> pages</b>: <%= value %></li>
  <% end %>
</ul>

<h4>Recently Clustered</h4>
<div id="preview-gallery">
  <% if (@recent_subject_ids.size > 0) %>
    <% @recent_subject_ids.each do |id| %>
      <div id="preview-<%= id %>" class="preview-holder">Loading…</div>
      <script type="text/javascript">
        $.ajax({
          url: "/subjects/preview/<%= id %>?eps=<%= @eps %>&min=<%= @min %>",
          cache: true,
          success: function(html){
            $("#preview-<%= id %>").html(html);
          }
        });
      </script>
    <% end %>
  <% else %>
    <div id="loading-data">
        <p>Try again in 30 seconds - data is still loading.</p>
    </div>
  <% end %>
</div>

<h4>Most Classifications(out of <%= @subject_count %>)</h4>
<div id="preview-gallery">
  <% if (@subject_ids.size > 0) %>
    <% @subject_ids.each do |id| %>
      <div id="preview-recent-<%= id %>" class="preview-holder">Loading…</div>
      <script type="text/javascript">
        $.ajax({
          url: "/subjects/preview/<%= id %>?eps=<%= @eps %>&min=<%= @min %>",
          cache: true,
          success: function(html){
            $("#preview-recent-<%= id %>").html(html);
          }
        });
      </script>
    <% end %>
  <% end %>
</div>
